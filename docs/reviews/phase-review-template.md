# Phase Review Report Template

이 템플릿은 각 Phase가 끝난 뒤 구현된 코드를 학습하고, 다음 작업자가 같은 흐름을 빠르게 이해할 수 있도록 남기는 report 양식입니다.
목표는 모든 코드를 정독하는 것이 아니라 핵심 요청 흐름, 데이터 저장 흐름, 실패 처리 흐름을 설명할 수 있게 되는 것입니다.

## 1. Phase 정보

- Phase:
- 관련 브랜치 / 이슈:
- 작성일:
- 작성자:

## 2. 이번 Phase에서 구현된 것

- 

## 3. 핵심 사용자 흐름

사용자 관점에서 어떤 흐름이 가능해졌는지 적습니다.

```text
예: 로그인 화면 입력 -> POST /api/v1/auth/login -> JWT 저장 -> 내 정보 조회
```

## 4. 핵심 요청 흐름

프론트엔드, API, service, repository, DB, 외부 API 사이의 이동을 적습니다.

```text
예:
POST /api/v1/auth/login
-> auth router
-> auth service
-> user repository
-> password verify
-> JWT 발급
-> response
```

## 5. 반드시 읽어야 할 코드

| 파일 | 읽어야 하는 이유 |
| --- | --- |
| `path/to/file` |  |

## 6. 가볍게 봐도 되는 코드

| 파일 | 이유 |
| --- | --- |
| `path/to/file` |  |

## 7. 데이터 저장 / 상태 변화

어떤 테이블, 상태, 메시지, 외부 맥락이 생성되거나 바뀌는지 적습니다.

- 

## 8. 실패 처리와 권한 처리

이 Phase에서 확인해야 할 실패 케이스를 적습니다.

- 인증 실패:
- 권한 실패:
- 입력 검증 실패:
- 외부 연동 실패:
- 기타:

## 9. 내가 설명할 수 있어야 하는 질문

이 질문에 답할 수 있으면 Phase를 이해한 것으로 봅니다.

- 

## 10. 아직 모르는 것 / 다음에 확인할 것

- 

## 11. 내가 직접 바꿔볼 작은 실험

작게 바꿔보면 이해가 깊어지는 실험을 적습니다.

- 

## 12. 다음 작업에 주는 영향

이 Phase 결과가 다음 Phase에 어떤 전제나 제약을 만드는지 적습니다.

- 
